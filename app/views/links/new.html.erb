<div class="container"> 

  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <h3>Add a New Link</h3>
  <%= form_tag '/links', method: :post do %>
<form class="form-inline">  
  <div class="form-group">
    <%= text_field_tag :slug, "", :placeholder => "Slug" %>  
  </div>
  <div class="form-group">
    <%= text_field_tag :target_url, "", :placeholder => "Target URL" %>  
  </div>
  <div class="form-group">
    <%= text_field_tag :slug, "", :placeholder => "Slug" %>  
  </div>    
  <div>
    <%= submit_tag 'Create Shortner Link', class: "btn btn-default" %>
  </div>
  <% end %>
</div>